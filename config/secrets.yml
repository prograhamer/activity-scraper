# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.
defaults: &defaults
  strava_client_id: <%= ENV["STRAVA_CLIENT_ID"] %>
  strava_client_secret: <%= ENV["STRAVA_CLIENT_SECRET"] %>
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>

development:
  <<: *defaults
  secret_key_base: d3ab5e65adbae14729de2adada958d378b6ac81b087fe786f20ff90c56ba9edc06277c1b1341c69195545c692f3cbefbc41ea569ff92b9fbae0a17970adde675

test:
  <<: *defaults
  strava_client_id: 123
  strava_client_secret: 123abc456def
  secret_key_base: 4e4637e083cbd625bc47a90617d05d060a3730d3e9e2c9b27bc79b52878ade51608e33ec728c21482d631a1317ca24694916d7f308c6f54ac5709aab874663e9

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  <<: *defaults
